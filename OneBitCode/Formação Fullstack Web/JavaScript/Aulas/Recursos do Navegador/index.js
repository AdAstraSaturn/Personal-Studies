// Recursos do navegador ( console )

let nome = "nome de usuário";
console.log(nome);
console.error(nome);
console.warn(nome);

// Window

window.alert(nome);
nome = window.prompt("Insira o seu nome"); // Não precisamos usar o window
let confirm = window.confirm(); // Não precisamos usar o window
console.log(nome);
console.log(confirm);

// Existem outras funções para o window
